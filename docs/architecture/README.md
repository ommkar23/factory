# Architecture decisions

Record significant architectural decisions in this directory as ADRs. Use a numbered filename such as `0001-shared-auth-contract.md` and include context, decision, consequences, owner, and date.

An ADR is required when code is extracted into a shared package before at least two applications need it, or when a change introduces a repository-wide platform constraint.
