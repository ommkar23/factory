# Add worktree location guidance

1. To-do: Update the project delivery instructions to require creating repository worktrees under `.worktrees/` and ensure that directory remains ignored by Git.
   To-verify: Confirm `AGENTS.md` explicitly names `.worktrees/` and `.gitignore` contains the `.worktrees/` entry.
