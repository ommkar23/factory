# Run all Factory web routes through one Home container

1. To-do: Create one Home web runtime that starts the existing Home, Weather, and Live Splash Next.js applications inside a single container and routes `/` to Home, `/weather` to Weather, and `/live-splash` to Live Splash in both development and production without changing the app scaffolds.
   To-verify: Add runtime tests proving exact and nested route prefixes, assets, authentication/application API paths, child-process startup failure, and graceful shutdown work through one public port.
2. To-do: Replace the separate Home, Weather, and Live Splash Compose services, dependency volumes, and gateway upstreams with one Home app container while preserving worktree isolation, hot reload, and the shared local Factory URL.
   To-verify: Add Compose and worktree lifecycle contract tests confirming local development creates exactly one web app container and serves `/`, `/weather`, and `/live-splash` through it.
3. To-do: Preserve environment-specific authentication through the unified container so development automatically bootstraps the configured test-token session for every app route while production disables development bootstrap and presents Google OAuth login through the shared Factory authentication routes.
   To-verify: Add browser and routing tests proving unauthenticated development visits to `/`, `/weather`, and `/live-splash` obtain the test session and return to the requested app, while production exposes Google login, never calls the development bootstrap endpoint, preserves same-origin HttpOnly cookies, and completes login/logout redirects for every route prefix.
4. To-do: Build one production Home image containing all three standalone Next.js runtimes and consolidate GitHub deployment automation so changes to any web app or shared web package publish only that image.
   To-verify: Add image and workflow contract tests confirming all app artifacts are present, only Home is deployed for web changes, and every relevant app, shared package, Docker, and router change triggers deployment.
5. To-do: Update Google Cloud infrastructure to expose all three web route families from `factory-home`, stop requiring separate Weather and Live Splash images or services, and document the deletion-protection-aware migration and rollback procedure.
   To-verify: Run Terraform formatting, validation, and infrastructure tests proving the load balancer uses one web backend, the API remains independently deployed, and obsolete web services and path backends are absent.
6. To-do: Update architecture and development documentation to describe unchanged app-local source scaffolding served by one Home container in local and production environments, including the test-token and Google OAuth authentication modes.
   To-verify: Confirm active documentation consistently specifies one web container and one web deployment for `/`, `/weather`, and `/live-splash`, with no instructions to run or publish three separate web services or use production development authentication.
7. To-do: Run repository verification and smoke-test the unified Home container in development and production modes.
   To-verify: Run `pnpm check`, build the unified image, and exercise Home, Weather, Live Splash, static assets, automatic development authentication, production Google login, logout, health endpoints, hot reload, and worktree cleanup through its single exposed port.
