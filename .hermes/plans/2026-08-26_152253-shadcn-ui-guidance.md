# Add shadcn/ui implementation guidance

1. To-do: Update the project instructions to require using and customizing shadcn/ui components when building application UIs and to discourage ground-up custom UI components.
   To-verify: Confirm the guidance is explicit in `AGENTS.md` and preserves the existing Storybook requirement for reusable UI components.
