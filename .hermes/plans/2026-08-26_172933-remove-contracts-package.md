# Remove the unused contracts package

1. To-do: Remove the empty `packages/contracts` package and clean its repository configuration, documentation, Docker, lockfile, and boundary-test references.
   To-verify: Confirm no contracts-package references remain, run the affected boundary test, and run `pnpm check`.
